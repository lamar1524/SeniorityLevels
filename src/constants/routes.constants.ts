import { RoutesConst } from 'src/app/core/interfaces/routes';

export const ROUTES: RoutesConst = {
  home: '',
  register: 'register',
  users: 'users',
  usersList: 'list',
  userProfile: 'profile',
};
